import SwiftUI

// Placeholder host required by Xcode for a UI-testing bundle; the tests drive Swipewalk.app.
@main
struct RunnerApp: App {
    var body: some Scene {
        WindowGroup { Text("Swipewalk desktop UI tests") }
    }
}
