import SwiftUI

// Placeholder host required by Xcode for a UI-testing bundle. The tests drive another app.
@main
struct RunnerApp: App {
    var body: some Scene {
        WindowGroup { Text("Swipewalk harness") }
    }
}
