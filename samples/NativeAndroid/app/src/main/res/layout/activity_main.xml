<?xml version="1.0" encoding="utf-8"?>
<!-- Launcher picker screen. Not part of the ground truth - see ../../../../ground-truth.views.json
     and ground-truth.compose.json for the "Pay a parking ticket" screens this leads to. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@color/white">

    <Button
        android:id="@+id/buttonViews"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="@string/picker_views_button" />

    <Button
        android:id="@+id/buttonCompose"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/picker_compose_button" />

</LinearLayout>
