<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!--
      No App Sandbox: Swipewalk runs adb, xcodebuild, xcrun devicectl and simctl, and reads the run history,
      which a sandboxed app cannot do. It is distributed outside the Mac App Store (signed and notarized).
    -->
    <dict>
    </dict>
</plist>
